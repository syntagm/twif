<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        xmlns:android   = "http://schemas.android.com/apk/res/android"
        id        = "za.co.syntagm.twif"
        version   = "0.0.1128">
    <name>TWIF</name>

    <description>
        TWIF Test app
    </description>

    <author href="http://www.syntagm.co.za" email="syntagm@icon.co.za">
        synAPPS Team
    </author>

    <!-- third party plugins
    <gap:plugin name="com.phonegap.plugins.barcodescanner" /> -->

    <!-- core plugins
    <gap:plugin name="cordova-plugin-battery-status" source="npm" />
    <gap:plugin name="cordova-plugin-camera" source="npm"/>
    <gap:plugin name="cordova-plugin-media-capture" source="npm"/>
    <gap:plugin name="cordova-plugin-console" source="npm"/>
    <gap:plugin name="cordova-plugin-contacts" source="npm"/>
    <gap:plugin name="cordova-plugin-device" source="npm"/>
    <gap:plugin name="cordova-plugin-device-motion" source="npm"/>
    <gap:plugin name="cordova-plugin-device-orientation" source="npm"/>
    <gap:plugin name="cordova-plugin-file" source="npm"/>
    <gap:plugin name="cordova-plugin-file-transfer" source="npm"/>
    <gap:plugin name="cordova-plugin-geolocation" source="npm"/>
    <gap:plugin name="cordova-plugin-globalization" source="npm"/>
    <gap:plugin name="cordova-plugin-inappbrowser" source="npm"/>
    <gap:plugin name="cordova-plugin-media" source="npm"/>
    <gap:plugin name="cordova-plugin-dialogs" source="npm"/>
    <gap:plugin name="cordova-plugin-vibration" source="npm"/> -->
		<gap:plugin name="cordova-plugin-inappbrowser" source="npm"/>

    <!--<gap:config-file platform="ios" parent="NSLocationWhenInUseUsageDescription" mode="replace">
        <string></string>
    </gap:config-file>

    <gap:config-file platform="ios" parent="UIStatusBarStyle" mode="delete"/>

    <gap:config-file platform="android" parent="/manifest">
        <supports-screens android:xlargeScreens="false" android:largeScreens="false" android:smallScreens="false" />
    </gap:config-file>


    <gap:config-file platform="android" parent="/manifest" mode="delete">
        <uses-permission android:name="android.permission.RECORD_VIDEO" />
        <uses-permission android:name="android.permission.VIBRATE" />
    </gap:config-file> -->

    <!-- universal preferences -->
    <preference name="phonegap-version"           value="cli-5.1.1" />
    <preference name="orientation"                value="portrait" />
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="true" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="load-url-timeout"           value="1000" />

    <!-- ios preferences -->
    <!-- control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="webviewbounce"              value="false" />
    <!-- if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="prerendered-icon"           value="true" />
    <!-- external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="stay-in-webview"            value="false" />
    <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="ios-statusbarstyle"         value="black-opaque" />
    <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="detect-data-types"          value="true" />
    <!-- ios: if set to true, app will terminate when home button is pressed -->
    <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="show-splash-screen-spinner" value="true" />
    <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="auto-hide-splash-screen"    value="true" />

    <!-- android preferences -->
    <!-- android: MIN SDK version supported on the target device. MAX version is blank by default. -->
    <preference name="android-minSdkVersion"      value="7" />
    <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->
    <preference name="android-installLocation"    value="auto" />
    <preference name="android-targetSdkVersion"   value="13" />
    <preference name="android-maxSdkVersion"       value="17" />
    <preference name="android-windowSoftInputMode" value="stateVisible|adjustResize" />
    <preference name="backupwebstorage" value="none"/>

    <!-- icons -->
    <!-- Define app icon for each platform. -->
    <icon src="icon.png" />
    <icon src="res/android/icon/drawable-ldpi-icon.png"   gap:platform="android"    gap:density="ldpi" />
    <icon src="res/android/icon/drawable-mdpi-icon.png"   gap:platform="android"    gap:density="mdpi" />
    <icon src="res/android/icon/drawable-hdpi-icon.png"   gap:platform="android"    gap:density="hdpi" />
    <icon src="res/android/icon/drawable-xhdpi-icon.png"  gap:platform="android"    gap:density="xhdpi" />
    <icon src="res/icon/ios/icon-57.png"            gap:platform="ios"        width="57" height="57" />
    <icon src="res/icon/ios/icon-72.png"            gap:platform="ios"        width="72" height="72" />
    <icon src="res/icon/ios/icon-57-2x.png"         gap:platform="ios"        width="114" height="114" />
    <icon src="res/icon/ios/icon-72-2x.png"         gap:platform="ios"        width="144" height="144" />
    <icon src="res/icon/windows-phone/icon-48.png"  gap:platform="winphone" />
    <icon src="res/icon/windows-phone/icon-173.png" gap:platform="winphone"   gap:role="background" />

    <!-- Define app splash screen for each platform. -->
    <gap:splash src="res/android/splash/drawable-land-ldpi-screen.png"       gap:platform="android" gap:density="ldpi" />
    <gap:splash src="res/android/splash/drawable-land-mdpi-screen.png"       gap:platform="android" gap:density="mdpi" />
    <gap:splash src="res/android/splash/drawable-land-hdpi-screen.png"       gap:platform="android" gap:density="hdpi" />
    <gap:splash src="res/android/splash/drawable-land-xhdpi-screen.png"      gap:platform="android" gap:density="xhdpi" />
		<gap:splash src="res/android/splash/drawable-port-ldpi-screen.png"       gap:platform="android" gap:density="ldpi" />
    <gap:splash src="res/android/splash/drawable-port-mdpi-screen.png"       gap:platform="android" gap:density="mdpi" />
    <gap:splash src="res/android/splash/drawable-port-hdpi-screen.png"       gap:platform="android" gap:density="hdpi" />
    <gap:splash src="res/android/splash/drawable-port-xhdpi-screen.png"      gap:platform="android" gap:density="xhdpi" />
    <gap:splash src="res/screen/ios/screen-iphone-portrait.png"         gap:platform="ios"     width="320" height="480" />
    <gap:splash src="res/screen/ios/screen-iphone-portrait-2x.png"      gap:platform="ios"     width="640" height="960" />
    <gap:splash src="res/screen/ios/screen-iphone-portrait-568h-2x.png" gap:platform="ios"     width="640" height="1136" />
    <gap:splash src="res/screen/ios/screen-ipad-portrait.png"           gap:platform="ios"     width="768" height="1024" />
    <gap:splash src="res/screen/ios/screen-ipad-landscape.png"          gap:platform="ios"     width="1024" height="768" />
    <gap:splash src="res/screen/windows-phone/screen-portrait.jpg"      gap:platform="winphone" />

    <!-- content src="http://www.w3.org/2010/05/video/mediaevents.html" / -->
    <access origin="*" />
</widget>
